{
  "name": "cleo-test-management",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  
"scripts": {
    "build": "npm run build -w cleo-cypress-codemod && npm run build -w cleo-test-management-reporting",

    "codemod": "npm run build && node packages/cleo-test-management-reporting/dist/cli/index.js codemod run"
  }
,
  "devDependencies": {
    "@types/node": "^25.7.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "commander": "^14.0.3"
  }
}
